export namespace LookupsActions {
  export class LoadAll {
    static readonly type = '[Lookups] Load All Lookups';
  }

  export class ClearAll {
    static readonly type = '[Lookups] Clear All Lookups';
  }

  export class ReloadTagTypes {
    static readonly type = '[Lookups] Reload Tag Types';
  }
}
