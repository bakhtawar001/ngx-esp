import { AssigneeFilterNamePipe } from './assignee-filter-name.pipe';

describe('AssigneeFilterNamePipe', () => {
  it('create an instance', () => {
    const pipe = new AssigneeFilterNamePipe();
    expect(pipe).toBeTruthy();
  });
});
