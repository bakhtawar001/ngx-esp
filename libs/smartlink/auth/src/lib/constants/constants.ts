export enum TemplateCodes {
  ResetPasswordChange = 'ERPC',
  ResetPassword = 'ERRP',
}

export enum ApplicationCodes {
  Encore = 'Encore',
}

export enum ErrorMessages {
  serverError = 'Something went wrong, please try again later.',
}
